# 戏韵·梨园谱系 — Python 依赖（pipeline 解析/分析 + backend FastAPI 服务）
# 版本固定自实际运行环境（conda env: llm）。复现：
#   conda create -n llm python=3.11 && conda activate llm
#   pip install -r requirements.txt
# 随后按 README「复现步骤」或 pipeline/run_all.sh 跑数据流水线，再 ./run.sh 起服务。

# 数据解析
PyMuPDF==1.26.7          # fitz：解析剧本 PDF

# 数值 / 数据
numpy==2.1.3
pandas==2.3.3
pyarrow==21.0.0          # parquet 读写
scipy==1.18.0            # 统计检验（Mann–Whitney/Kruskal–Wallis/JS 距离等）

# 机器学习 / NLP / 网络
scikit-learn==1.7.2      # 逻辑回归/LDA/KMeans/MDS 等
networkx==3.5            # 角色关系网络
jieba==0.42.1            # 中文分词与词性标注

# 后端服务
fastapi==0.135.1
uvicorn==0.36.0
pydantic==2.12.5

# AI 助手（DeepSeek，OpenAI 兼容端点）
openai==2.24.0
python-dotenv==1.0.1     # 读取 backend/.env
