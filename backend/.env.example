# AI 助手配置示例。复制为 backend/.env（已在 .gitignore 中忽略），或直接 export。
# 也可不配这里，在桌面应用 AI 面板右上角「设置」里填 Key、选模型（免重启）。
#
# 助手为「工具调用智能体」：模型自主调用后端查询工具检索 data/processed 真实数据后作答。
# 工具调用需要模型支持 function calling —— 默认 deepseek-chat 支持；deepseek-reasoner
# 不支持，会自动降级为静态接地单轮。

# === 默认：DeepSeek 原生（推荐，开箱即用） ===
DEEPSEEK_API_KEY=sk-你的key
DEEPSEEK_BASE_URL=https://api.deepseek.com
# 可选：默认模型（不填则用推荐的 deepseek-chat）
# DEEPSEEK_MODEL=deepseek-chat

# === 可选：ZenMux 聚合（一把 Key 通调 GPT / Claude / Gemini / Qwen 等） ===
# 模型 id 含 "/"（如 openai/gpt-5.4）即路由到 ZenMux。
# ZENMUX_API_KEY=sk-ai-v1-你的key
# ZENMUX_BASE_URL=https://zenmux.ai/api/v1
